from auth import *
from config import *
import datetime
import time

get_html(get_data(1))



# now = str(datetime.datetime.now()).split(' ')[1].split('.')[0].split(':')
# TIME_TO_PUSH = [['08', '30', '01'], ['15', '30', '01'], ['18', '30', '01']]

# while True:
#   now = str(datetime.datetime.now()).split(' ')[1].split('.')[0].split(':')
#   if now == TIME_TO_PUSH[0] or now == TIME_TO_PUSH[1]  or now == TIME_TO_PUSH[2]:
#     get_html(get_data(1))
#     time.sleep(5)
    


